<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/compose_ic_launcher"
        android:label="@string/compose_app_name"
        android:roundIcon="@mipmap/compose_ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MatrixAndroid">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/compose_app_name"
            android:theme="@style/Theme.MatrixAndroid">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activitys.FirstActivity"/>
        <activity android:name=".activitys.SecondActivity"/>
        <activity android:name=".activitys.ThirdActivity"/>
    </application>

</manifest>



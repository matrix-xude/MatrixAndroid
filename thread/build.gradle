plugins {
    id 'matrix.android.common'
    id 'matrix.android.module'
    id 'matrix.android.arouter'
}

android {
    // 绝大部分配置已在公共插件中处理：
    // 1. matrix.android.common: 基础SDK版本、namespace、ViewBinding、Java版本、buildTypes、测试配置等
    // 2. matrix.android.module: 根据 isModularize 自动切换 application/library 以及 manifest 路径
    // 3. matrix.android.arouter: arouter 的 kapt 配置
}

dependencies {
    // 公共依赖（:common, arouter, junit等）已在插件中统一引入
}
